///|
fn main {
  // STATUS: NEEDS_ADJUSTMENT - bootstrap still wires one demo world and demo mobs
  // directly; a menu/config path should own this once multiple play modes exist.
  register_world_types()
  register_launcher()
}
